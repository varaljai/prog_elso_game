#ifndef CONSTS_HPP_GUARD
#define CONSTS_HPP_GUARD

const float WINDOW_WIDTH = 768.;
const float WINDOW_HEIGHT = 1024.;
const int FRAME_TIME = 16;  // kb 60 fps
const float PADDLE_WIDTH = 180.;
const float PADDLE_HEIGHT = 10.;
const float DEFAULT_PADDLE_SPEED = 600.;


#endif // CONSTS_HPP_GUARD